{{- if .Values.sender.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "calendar.fullname" . }}-sender
  labels:
    {{- include "calendar.labels" (dict "top" . "component" "sender") | nindent 4 }}
spec:
  replicas: {{ .Values.sender.replicaCount }}
  selector:
    matchLabels:
      {{- include "calendar.selectorLabels" (dict "top" . "component" "sender") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "calendar.selectorLabels" (dict "top" . "component" "sender") | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/sender-configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- include "calendar.imagePullSecrets" . | nindent 6 }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "calendar.waitForRabbitmq" . | nindent 8 }}
      containers:
        - name: sender
          image: {{ include "calendar.image" (dict "top" . "image" .Values.sender.image) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            {{- include "calendar.rabbitPasswordEnv" . | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/calendar
              readOnly: true
          resources:
            {{- toYaml .Values.sender.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "calendar.fullname" . }}-sender-config
      {{- with .Values.sender.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.sender.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.sender.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
